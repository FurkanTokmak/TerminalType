# TerminalType

A terminal-based typing speed test built with TypeScript. Type random quotes, get real-time WPM tracking, and instant color-coded feedback — all from your terminal.

![screenshot](screenshot.png)

## Usage

```bash
npx tsx index.ts
```